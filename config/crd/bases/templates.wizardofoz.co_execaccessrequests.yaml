---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: execaccessrequests.templates.wizardofoz.co
spec:
  group: templates.wizardofoz.co
  names:
    kind: ExecAccessRequest
    listKind: ExecAccessRequestList
    plural: execaccessrequests
    singular: execaccessrequest
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: ExecAccessRequest is the Schema for the execaccessrequests API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: ExecAccessRequestSpec defines the desired state of ExecAccessRequest
            properties:
              templateRef:
                description: Configures which Template this `ExecAccessRequest` is
                  for.
                properties:
                  apiVersion:
                    description: Defines the "APIVersion" of the resource being referred
                      to. Eg, "apps/v1".
                    type: string
                  kind:
                    description: Defines the "Kind" of resource being referred to.
                    enum:
                    - ExecAccessTemplate
                    - AccessTemplate
                    type: string
                  name:
                    description: Defines the "metadata.name" of the target resource.
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
            required:
            - templateRef
            type: object
          status:
            description: ExecAccessRequestStatus defines the observed state of ExecAccessRequest
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
