apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.10.0
  creationTimestamp: null
  name: accesstemplates.crds.wizardofoz.co
spec:
  group: crds.wizardofoz.co
  names:
    kind: AccessTemplate
    listKind: AccessTemplateList
    plural: accesstemplates
    singular: accesstemplate
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AccessTemplate is the Schema for the accesstemplates API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: AccessTemplateSpec defines the desired state of AccessTemplate
            properties:
              allowedGroups:
                description: AllowedGroups lists out the groups (in string name form)
                  that will be allowed to  into the target pod.
                items:
                  type: string
                type: array
              command:
                description: Command is used to override the .Spec.containers[0].command
                  field for the target Pod and Container. This can be handy in ensuring
                  that the default application does not start up and do any work.
                  If set, this overrides the Spec.conatiners[0].args property as well.
                items:
                  type: string
                type: array
              defaultDuration:
                default: 1h
                description: "DefaultDuration sets the default time that an `AccessRequest`
                  resource will live. Must be set below MaxDuration. \n Valid time
                  units are \"ns\", \"us\" (or \"µs\"), \"ms\", \"s\", \"m\", \"h\"."
                type: string
              maxCpu:
                anyOf:
                - type: integer
                - type: string
                description: Upper bound of the CPU that an AccessRequest can make
                  against this tmemplate for the primary container.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              maxDuration:
                default: 24h
                description: "MaxDuration sets the maximum duration that an `AccessRequest`
                  resource can request to stick around. \n Valid time units are \"ns\",
                  \"us\" (or \"µs\"), \"ms\", \"s\", \"m\", \"h\"."
                type: string
              maxMemory:
                anyOf:
                - type: integer
                - type: string
                description: Upper bound of the memory that an AccessRequest can make
                  against this template for the primary container.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              maxStorage:
                anyOf:
                - type: integer
                - type: string
                description: Upper bound of the ephemeral storage that an AccessRequest
                  can make against this template for the primary container.
                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                x-kubernetes-int-or-string: true
              resources:
                description: If supplied these resource requirements will override
                  the default .Spec.containers[0].resource requested for the the pod.
                  Note though that we do not override all of the resource requests
                  in the Pod because there may be many containers.
                properties:
                  limits:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: 'Limits describes the maximum amount of compute resources
                      allowed. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                  requests:
                    additionalProperties:
                      anyOf:
                      - type: integer
                      - type: string
                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                      x-kubernetes-int-or-string: true
                    description: 'Requests describes the minimum amount of compute
                      resources required. If Requests is omitted for a container,
                      it defaults to Limits if that is explicitly specified, otherwise
                      to an implementation-defined value. More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/'
                    type: object
                type: object
              targetRef:
                description: TargetRef provides a pattern for referencing objects
                  from another API in a generic way.
                properties:
                  apiVersion:
                    description: "Defines the \"APIVersion\" of the resource being
                      referred to. Eg, \"apps/v1\". \n TODO: Figure out how to regex
                      validate that it has a \"/\" in it"
                    type: string
                  kind:
                    description: Defines the "Kind" of resource being referred to.
                    enum:
                    - Deployment
                    - DaemonSet
                    - StatefulSet
                    type: string
                  name:
                    description: Defines the "metadata.Name" of the target resource.
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
            required:
            - allowedGroups
            - defaultDuration
            - maxDuration
            - targetRef
            type: object
          status:
            description: AccessTemplateStatus defines the observed state of AccessRequest
            properties:
              conditions:
                description: Current status of the Access Template
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource. --- This struct is intended for direct
                    use as an array at the field path .status.conditions.  For example,
                    \n type FooStatus struct{ // Represents the observations of a
                    foo's current state. // Known .status.conditions.type are: \"Available\",
                    \"Progressing\", and \"Degraded\" // +patchMergeKey=type // +patchStrategy=merge
                    // +listType=map // +listMapKey=type Conditions []metav1.Condition
                    `json:\"conditions,omitempty\" patchStrategy:\"merge\" patchMergeKey:\"type\"
                    protobuf:\"bytes,1,rep,name=conditions\"` \n // other fields }"
                  properties:
                    lastTransitionTime:
                      description: lastTransitionTime is the last time the condition
                        transitioned from one status to another. This should be when
                        the underlying condition changed.  If that is not known, then
                        using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: message is a human readable message indicating
                        details about the transition. This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: observedGeneration represents the .metadata.generation
                        that the condition was set based upon. For instance, if .metadata.generation
                        is currently 12, but the .status.conditions[x].observedGeneration
                        is 9, the condition is out of date with respect to the current
                        state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: reason contains a programmatic identifier indicating
                        the reason for the condition's last transition. Producers
                        of specific condition types may define expected values and
                        meanings for this field, and whether the values are considered
                        a guaranteed API. The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                        --- Many .condition.type values are consistent across resources
                        like Available, but because arbitrary conditions can be useful
                        (see .node.status.conditions), the ability to deconflict is
                        important. The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              podName:
                description: The Target Pod Name where access has been granted
                type: string
              ready:
                description: Simple boolean to let us know if the resource is ready
                  for use or not
                type: boolean
              roleBindingName:
                description: The name of th RoleBinding created for this temporary
                  access request
                type: string
              roleName:
                description: The name of the Role created for this temporary access
                  request
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
